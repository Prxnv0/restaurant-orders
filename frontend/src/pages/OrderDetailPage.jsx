// Placeholder — Milestone 5 will implement the real order detail with
// lines, timeline, voiding, collaborators, notes, and state transitions.
export default function OrderDetailPage() {
  return (
    <div className="card">
      <h2>Order Detail</h2>
      <p className="muted">Order detail with lines, history, collaborators, voiding, and status transitions will appear here (Milestone 5).</p>
    </div>
  );
}