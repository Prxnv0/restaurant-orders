// Placeholder — Milestone 4 will implement the real orders list with
// search, filter, sort, pagination, and role-scoped visibility.
export default function OrdersPage() {
  return (
    <div className="card">
      <h2>Orders</h2>
      <p className="muted">Orders list with search, filter, sort, and pagination will appear here (Milestone 4).</p>
    </div>
  );
}